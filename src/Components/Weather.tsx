import * as C from "../AppStyle";


//sensacao termica, humidade, temperatura, minima e maxima, nome/pais, nascer do sol/por do sol, tempo(ensolarado), vento(velocidade/direção).
export const Weather = () => {
    return(
        <C.WeatherContainer>
            
        </C.WeatherContainer>
    )
}